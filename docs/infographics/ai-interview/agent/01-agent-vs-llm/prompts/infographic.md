Create one publication-ready Chinese educational infographic.

## Output

- Use case: infographic-diagram
- Topic: Agent 与 LLM 的本质区别
- Aspect ratio: 16:9 landscape
- Language: Simplified Chinese
- Layout: bento-grid, with one large hero card and three supporting cards
- Style: hand-drawn-edu, pastel cards variant
- Audience: software engineers preparing for AI Agent interviews

## Visual Direction

Use a warm cream paper background, deep charcoal text, and no more than four macaron colors: blue, mint, peach, and lavender. Draw friendly hand-sketched cards, arrows, a small robot/brain icon, a tool icon, and a shield icon. Keep the illustration lightweight and professional. Shapes may have a subtle hand-drawn wobble, but all Chinese text must use a clean, bold, print-like sans-serif treatment rather than cursive handwriting.

Create a clear visual hierarchy:

1. A prominent title across the top.
2. A small left card showing the single-pass LLM flow.
3. A large center-right hero card showing the Agent execution loop.
4. Two compact bottom cards for system layers and production guardrails.

The Agent loop is the dominant visual. Use thick arrows to make the feedback cycle understandable at a glance.

## Exact On-Image Text

Render only the following Chinese text. Preserve every character exactly. Do not add paragraphs, explanations, slogans, or random labels.

Title:
`Agent 与 LLM：差别在执行闭环`

Card 1 heading:
`LLM：生成与推断能力`

Card 1 flow:
`输入 → 生成 → 输出`

Hero card heading:
`Agent：围绕 LLM 的任务执行系统`

Hero loop labels, clockwise:
`目标与状态 → 模型决策 → 策略校验 → 工具执行 → 结果反馈 → 继续或停止`

Hero callout:
`工具调用 ≠ Agent`

Bottom card 1 heading:
`三层职责`

Bottom card 1 labels:
`能力层 / 控制层 / 系统层`

Bottom card 2 heading:
`生产护栏`

Bottom card 2 labels:
`预算 / 最小权限 / 审计 / 人工接管`

Small footer note:
`固定路径偏 Workflow；动态局部决策体现 Agent 特征`

## Accuracy Constraints

- Depict the LLM as one decision component inside the Agent system, not as the entire system.
- Depict tool execution as performed by a controlled runtime after policy validation.
- Do not imply that autonomy is unlimited or always desirable.
- Do not depict Agent and Workflow as an absolute binary; the footer should suggest a continuum.

## Avoid

- No dense prose, tiny text, tables, pseudo-code, logos, watermarks, signatures, citations, or UI screenshots.
- No extra English words other than `Agent`, `LLM`, and `Workflow` already listed above.
- No repeated title, misspelled labels, garbled Chinese, or decorative text.
- No photorealism, glossy 3D, dark background, crowded doodles, or excessive characters.
